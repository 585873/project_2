<h1>Student list </h1>
  <% @students.each do |student| %>
  <div>
    <h2><%= link_to student.name, student_path(student) %> - <%= Instructor.find(student.instructor_id).name %></h2>

<% end %>
</div>
